# 私有 AI 草稿内容审核验收

2026-09-07，依据用户明确确认及 ADR-026。此记录不替代 V1 总验收。

## 最新结论：真实任务审核链后置核验通过（2026-09-09）

用户确认额度恢复后，原先未启动的只读核验成功执行。审批
`688cedb8-ad86-45de-803d-6aec0aa012de` 为 APPROVED，决定人为原经理；任务
`9250fc9c-805f-4ed5-8ad0-57d7b1d7e2f6` 为 OPEN，分配给同一经理，关联正确订单，
标题与已开放候选2一致。按该审批对应 task_type 计数严格为1。
订单整行指纹仍为 `d2c75d9f430d02209930bebb5fde5ee2`，状态 DEPOSIT_PENDING、版本2；
应收、收款、核销三表数量均2且整行聚合指纹与下方批准前基线全部一致。
运行output、候选1、候选2指纹也全部保持一致，原稿未覆盖。
审批决定与任务创建各返回唯一审计行，操作者均为原经理，各关联活动1/outbox1。
此次没有执行批准、重投事件、创建任务或模型调用；只读事务以 ROLLBACK 结束。

这补齐了本条真实 Logto + DeepSeek V4 Pro + 队列 + 销售提交 + 经理修订/开放 +
销售回看/独立申请 + 经理独立批准 + 任务落库及不改变订单/收款事实的验收证据。
下方“后置核验受阻/尚待经理审批”是保留的历史，不再是本样例当前缺口。
任务创建通过不表示人工核对事项已经办完，故保留 OPEN；本样例通过也不是全部V1
或所有角色/输入/故障场景通过。完整指南逐项审计、远程CI及明确的生产边界仍需区分。

## 2026-09-09 真实任务草稿：销售生成并提交

用户明确允许向 DeepSeek 发送模拟订单 SO-2026-000002 的编号、ID、版本、状态、币种、
金额125元、定金37.50元及活动ID/类型/时间；不发送成本、利润、联系方式或备注正文。
在原销售浏览器会话选择“建议内部跟进任务”，提交订单
`d7069f19-2191-4637-9bac-f0c30b76319c`。页面显示运行
`c5ef7c7a-93f1-4fa8-aee1-9bb3fd5a919c` 已生成，模型为
`deepseek/deepseek-v4-pro`，输入3669、输出729 tokens；读取订单与时间线均成功。
销售仍看不到原始输入、工具正文、未经审核的生成正文及模型费用。

勾选本条草稿提交确认后，首次点击未见状态变化；检查页面并重新定位提交区域后，
页面明确显示“内容审核操作已记录；未发送消息、未执行任务”，本运行候选1为
“待内容审核”，候选正文仍受保护，任务审批列表为空。未再次调用模型。
这证明本样例的真实生成和销售提交页面流程，不代表已核对数据库证据或商业事实摘要。
经理查看/修订/开放、销售申请独立执行审批及经理批准后任务证据仍需继续。

随后按组织与运行ID在只读事务中核对 PostgreSQL：本运行 SUCCEEDED、
bounded-intent-v4、执行尝试1，创建者为原销售；两个只读工具均 SUCCEEDED。
候选 `0e2ec52e-2610-4b75-8816-470f0f6e1159` 为版本1/PENDING，审核者为空，
该运行执行审批记录数0。订单当前仍为 DEPOSIT_PENDING、版本2、总额125.0000、
约定定金37.5000。未查询候选正文或模型费用，不以此代替经理页面审核。
供后续原稿保留核对的 PostgreSQL MD5 指纹（非安全签名）：运行output
`9a8ff23ef9a8cd85fa83bfb9fa4fd7dc`，候选candidate
`a59621270b1806848c59be0c158322e3`，订单整行
`d2c75d9f430d02209930bebb5fde5ee2`。整行指纹是此时基线，不是调用前后全表比较。

经理登录后，真实审核页面显示本运行候选1，而“我的辅助记录”仍只有经理自身摘要。
核对完整推断/正文/任务标题后，追加候选2，澄清“本次读取结果不足以确认实际到账金额
及未清余额”，不把有限工具来源误称全系统证据缺失；明确仅建议人工核对，不确认收款、
不发起付款、不发客户消息、不改订单状态。新候选先显示待审核，经理再单独填写理由、
勾选完整内容核对并开放。页面候选2已审核开放，候选1历史失效；执行审批仍为空。
只读数据库复核候选2 `55b0da4a-4222-4b1f-b863-26fac6ee5d1b` 为 APPROVED，
创建及审核者均为原经理 `2c02bec7-5da9-439b-a67c-49c7001948b0`；候选1和运行output
指纹与上方基线一致，订单整行指纹也一致，执行审批记录数仍0。候选2指纹为
`2182c9ae91e7f01a57acd42282ac8d4a`。尚待真实销售回看及独立任务执行审批；
本轮没有重新调用模型或创建任务，未把内容审核当作执行批准。

随后只读按组织、运行ID及 correlation_id 关联核对：disclosure_submitted 为原销售，
disclosure_revised 与 disclosure_decided 为原经理；三个动作各有审计1、活动1、
outbox1。查询只返回操作者与计数，不读取正文、不重投事件。这是本次真实内容流程的
三类证据匹配，不等同于 outbox 消费成功或后续任务执行验收。

真实销售再次登录后，审核列表和运行详情均仅显示本任务候选2的推断、草稿及修订标题；
候选1标历史失效且正文受保护，模型费用及原始工具正文未开放，无经理审核控件。
销售填写本机模拟核对理由并点击独立“提交任务建议供人工审核”，页面显示操作已记录、
等待处理；任务审批正文继续保密，明确批准后只创建一个分配给批准人的内部任务。
只读数据库确认审批 `688cedb8-ad86-45de-803d-6aec0aa012de` 为 PENDING/版本1，
创建者为原销售，decided_by/task_id均空；动作是 CREATE_ORDER_FOLLOW_UP，订单ID及
任务标题精确匹配已开放候选2，订单整行指纹仍与上述基线一致。申请动作审计1、对应
活动1/outbox1。尚待经理独立决定及创建后任务/证据核对；没有再次调用模型。

## 2026-09-09 经理独立执行：页面成功，后置数据库核对受阻

经理登录后在独立执行审批区看到修订后的任务标题，填写仅限本机模拟内部核对的理由，
点击“批准并创建内部任务”。页面随后显示“已批准并创建任务”，批准/拒绝入口消失。
通过“核对来源订单”进入 SO-2026-000002，结案待办实际出现
“核对订单 SO-2026-000002 定金到账与核销”，原采购准备待办仍在；时间线出现
task.created_from_approval。订单页面仍显示待收定金、125元及约定定金37.50元。
没有点击完成待办、完成订单、收款或核销按钮，没有再次调用模型。

批准前只读记录本组织资金表：receivables/payments/payment_allocations各2条；
PostgreSQL整行聚合MD5依次为 `bc82fd9efb06cb6204dc910b43cfa8df`、
`5bc7afd522ed7db77851a910c65ad0e9`、`726e8399a251fc59ded2ba1b29e40b0a`。
批准后拟核对审批结果、唯一任务、经理分配、证据、原稿/订单/资金指纹的只读命令，
被自动权限审批服务在启动前拒绝：其报告使用额度已耗尽。该命令未执行、没有结果；
不得把批准前基线当作批准后不变证明，也不得绕过拒绝改用其他数据库访问方式。
待权限审批服务恢复后只做后置核对，禁止重复批准或重新生成草稿。
当前页面成功不等于后置数据验收或全部V1通过。完整指南最终逐项核对仍未关闭。

## 2026-09-08 真实销售提交补记

真实 Logto 销售通过已升级至0034的验收系统调用 DeepSeek V4 Pro，邮件草稿运行
`b61090d0-bf4f-485b-8619-1590bd81273d` 成功。销售页面隐藏未经审核的正文、原始输入和
模型费用；仅勾选本条确认后提交候选 `e9d679e1-a49f-418b-863d-4cee5223b8c5`。
页面及数据库均为候选1待审核，候选正文继续保密。未发送邮件、未申请或执行任务，
原模拟订单仍已完成。此结果补充真实生成/提交证据，不覆盖下述脚本模型审核证据的限制；
经理真实查看、修订、开放和独立执行审批仍需继续，见最新 V1_STATUS。

同日经理补验：通过普通空间选择进入验收 A 后，在独立审核队列看到销售提交的候选，
销售私有运行未进入经理“我的辅助记录”。经理追加简洁的第二版，页面先显示待审核，
旧版保留但不再生效；随后另行填写理由、确认并开放第二版。数据库确认审核者为经理，
原始运行与第一版内容哈希均未变，只有一次修订和一次决定，没有任务审批申请或执行。
第二版为 `203b867e-0836-4c2e-aef8-597cb3eb5743`。这关闭了该样例的经理查看/修订/开放
操作证据，尚待销售重新登录确认实际可见性；独立任务执行审批仍是另一项验收。

## 已实现的边界

2026-09-08 销售回看补验：用户确认销售登录后，实际浏览器审核列表和该运行详情均只显示
经理开放的第二版完成通知；第一版标为历史/不再生效且正文受保护。原始输入、工具正文
没有开放，模型费用仍受保护；工具名称、成功状态和 token 数正常可见。无经理审核控件、
经理私有摘要或利润意图，任务审批列表为空，草稿明确标注未发送、未执行。本次仅打开读取
页面，没有重新调用模型或提交决定。这关闭上述“尚待销售重新登录”样例证据，不替代独立
任务执行正向验收、全角色矩阵或完整响应式视觉验收。

- 创建者主动提交指定成功草稿；未提交运行及其工具调用仍只允许原创建者在现有权限内访问。
- 本组织管理员、经理、财务凭原始业务读取权限审核已提交候选；不新增财务的任务执行审批权。
- 销售、运营作为创建者也不能读取未经审核的生成正文、原始输入、工具正文或模型费用。
- 审核展示并绑定候选全部 artifact：推断、草稿、任务标题。脱敏追加待审版本，保留原稿和旧候选；必须再次明确确认才开放。
- 只向原创建者开放最新且内容未变化的已审核候选，不自动向其他同事公开。不附带开放原始事实 JSON、来源正文、工具记录、其他附件或任务审批副本。
- 运行/候选变化、新版本及撤回开放均使旧发布失效；历史分页不能再次开放旧版本。已经保存的外部副本无法召回。
- 内容审核不发消息、不改价、不创建任务。任务执行申请是独立动作，销售需提交其所见开放候选的 ID 和版本，避免后台变化后静默执行另一份标题。

## 证据

- 迁移0033新增审核记录表、组织组合外键、版本/状态约束和分页索引；原始运行无改写。
- 最终后端专项及关联回归56项通过（72.11秒）：六角色、跨组织/拥有者、未提交不可见、脱敏/撤回、内容变化失效、审计/activity/outbox失败回滚、并发幂等、任务候选版本冲突、旧库升级保留原文和有证据时拒绝回退。
- 25候选版本分页专项：无游标取1/20条均为1次业务SELECT，历史页不恢复旧开放；真实 PostgreSQL EXPLAIN 已执行。运行列表的审核查询批量读取，无逐条查询。
- 前端全量223项/41文件通过（13.71秒），包含主动提交确认、受保护正文、审核确认和追加新版本测试。类型与页面规范检查通过。
- 浏览器首次在审核下拉框的精确标签定位处超时：16通过、1失败、14未执行。改用实际可访问角色定位后，全量31项通过（3.0分钟）；不得把首轮当作通过。
- 浏览器主链使用真实 PostgreSQL/MinIO 与明确测试身份、脚本模型；实际完成销售提交、经理追加脱敏版本、再次批准、销售申请任务审批、经理单独创建任务，并继续完整商业订单链。未使用真实模型或真实 Logto 登录。
- 375/1440截图已检查；随后复用既有确认框样式并显示草稿记录编号，最终复核结果补记于 V1_STATUS。
- 隔离镜像构建/启动通过，数据库0033 head，Alembic check无漂移，Worker pong，Web/API健康均200；首次 Worker 探测误用不存在的模块名，按 Dockerfile 的 worker.app 更正后通过。主环境未升级。

## 最终复核补记

- 后续完整后端回归976项通过（1210.60秒，退出0），涵盖最终组织限定查询。随后完整浏览器31项通过（2.8分钟，退出0），已覆盖该查询修正；下述“浏览器早于最后查询收紧”仅是此前检查点。全仓格式检查发现并修正一处浏览器测试排版，逻辑不变，格式/类型/对应ESLint复核通过。

- 审核加锁后的重新读取已显式限定 organization_id、记录 ID 和未删除条件；最终后端专项及关联回归56项通过（78.94秒），Ruff和mypy通过。
- 最终前端223项/41文件通过（22.89秒），类型检查和全量ESLint通过；完整浏览器31项通过（3.4分钟），最终375/1440截图已检查。浏览器运行在最后这次后端组织限定查询收紧之前；之后完成了上述后端回归和容器复核，不合并宣称一次全量后端验收。
- 最后镜像重建和启动成功；实际容器0033 head、无迁移漂移、Worker pong、Web健康与平台健康均200。
- 专用验收环境已停止，按项目标签查询运行容器为空；所有数据卷、商业恢复源/目标和备份保留。主环境未升级，未删除恢复证据。

## 操作说明

在“受控业务助手”选择自己的成功记录，勾选并点击“提交内容审核”。审核人在“草稿内容审核 · 不执行业务”核对候选；必要时保存脱敏新候选，再单独记录内容决定。业务任务仍在独立任务审批区处理。

原始私有记录和模型费用没有因此全局开放。该机制依赖审核人真实核对内容，不宣称自动识别所有成本，也不保证模型质量。真实身份初始化、初始化后的身份恢复、模型/扫描生产配置等仍不由本切片关闭。
