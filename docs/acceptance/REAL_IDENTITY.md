# 本机真实身份验收记录

日期：2026-09-07。仅限本机隔离验收，不是生产发布证书。

## 退税模拟结案核对（2026-09-07 19:15）

用户上传后，REF-2026-000001 的商业发票与提单均显示 AVAILABLE，缺件为无。
沿原经理会话由 DOCUMENTS_PENDING 确认就绪、模拟申报、办理中及到账，最终 REFUNDED。
原组织限定只读事务确认预计100.0000 CNY、合成实收90.0000 CNY、差额10.0000 CNY，
申报及到账日期均2026-09-07，回执 SIM-REFUND-SUBMIT-20260907-A。
这是合成验收数据，没有实际税务申报、银行到账或法定应退权益；理由逐步注明此边界。
创建、请求资料、就绪、申报、办理、到账六条审计均为原经理用户，
按组织、对象、动作及 correlation/request ID 对照，各有且仅有活动1、outbox事件1。
终态页面移除状态、跟进、上传和替换入口，保留文件下载、独立审核及时间线。
未审核文件/正文没有自动开放。此补记取代下文退税未上传/NOT_READY的历史检查点，
不代表全部角色矩阵或V1总验收完成。

## 环境与初始化

### 销售完整商业样例页面复核（2026-09-07 19:31）

用户重新销售登录后，在限定本机应用标签检查Q-2026-000001、SO-2026-000001、
PO-2026-000001、SHP-2026-000001与REF-2026-000001。销售金额1250、定金375、
尾款875、数量10及交付/结清状态可见；报价/订单成本毛利、采购金额、未审核询盘/
条款/行说明/采购正文/收款备注/历史正文均不可见。出货文件名保密且无下载入口，
退税附件下载禁用；退税合成100/90/10金额、日期和识别回执可见，正文与六步历史保密。
未执行披露、财务写入或其他业务状态命令；这是页面证据，不冒充全部API攻击矩阵。

发现OrderFinance未按权限隐藏财务命令入口，修正为逐项检查当前会话成员权限，
同时保护应收生成、任务处理和经理结案入口，保留只读查询与归档提示。
新增七种独立权限组合测试先失败7项，修正后专项14项、全前端237项/42文件通过；
TypeScript、ESLint通过。代码简化复核保留显式权限判断，不改变后端规则。
验收Web构建部署通过，镜像492b4f128ffca749fdcd9ca94dc862fac8118c5ce9ea3dadfa5a698eeace7219，
healthy及health/platform-health均200；保留原销售会话后重新打开订单，确认收款/
核销/冲销入口消失且读权限与保密内容不变。只重建Web，未清理恢复容器或数据卷。
本轮未重跑完整Playwright或后端全量；后续仍需经理指定内容审核、复核销售开放范围，
运营/只读等角色覆盖与总指南收口，不能宣布V1总验收完成。

- 复用用户已初始化的本机 Logto，ES384/P-384；验收 Web 为 localhost:3300，
  `E2E_AUTH_MODE` 为空。密码由用户输入，凭据不写入本文件。
- 首次经理真实登录后，尚无本地成员时页面明确拒绝进入业务空间。
- 用户授权后执行一次 `apps/api/scripts/identity_acceptance_fixture.py`，依据 ADR-018
  的运维 bootstrap 边界创建合成身份映射，不是生产成员管理接口。
  CLI 必须明确 `--confirm-local-identity-fixture`，严格检查 acceptance 配置、实际库名和用户，
  锁定身份表；只要已有任意组织或用户即拒绝，不能重跑覆盖或补授权。
- A：`f07ced57-2bae-4973-9dd9-487466417653`；经理 MANAGER，销售 SALES。
  B：`65076f9e-f0b5-4696-bccc-6653a5f2bc3e`；同一经理为 VIEWER。
  未授予 ADMIN，未添加真实业务组织。
- 该脚本专项 15 项通过（4.98 秒，前一施工检查点）；目标配置拒绝、重复初始化保留、
  三类证据失败全部回滚均覆盖。脚本 narrow simplifier 复核保留显式事务和固定边界，
  未引入新的抽象或依赖。此后未再次运行初始化。

## 真实经理会话与组织隔离

- 用户真实登录后能且仅能选择 A、B 两个有效成员组织。
- 在 A 从页面登记“真实登录隔离验收 A-20260907”，线索
  `c29bddec-b492-48e7-92cf-07eaa7aa37de`，NEW/v1。
- 切换 B 后列表 0 条；保持该精确详情地址，页面返回 `LEAD_NOT_FOUND`。
  这是实际 UI 观察，不宣称已抓取 HTTP 状态码或完成所有端点的真实令牌测试。
- 发现 B 的只读页面仍显示创建入口，修复线索页按 `lead.write` 和 `lead.convert`
  分别控制创建/状态/转换；查询失败与等待时不开放操作，降权卸载表单。
- 仅重新构建、更新验收 Web；沿用原加密会话密钥，未改数据库或重置登录。
  更新后真实 B 会话复验：0 条、精确详情不可读、两个创建入口均不存在。
- 切回 A：原线索仍为 NEW/v1、列表 1 条，经理创建和状态操作正常显示。
  随后通过页面退出登录，返回 localhost:3300 登录页；重新点击登录显示 Logto
  用户名/密码表单，没有直接恢复经理业务空间。已交给用户进行销售账号登录，
  未读取或代填密码。这证明本次页面退出行为，不证明全部服务端会话撤销场景。
- 只读 PostgreSQL 核对：该线索 created_by 与 audit actor 均为经理本地用户
  `2c02bec7-5da9-439b-a67c-49c7001948b0`；创建审计 correlation
  `45c747e8-6d03-45b9-aace-6661eeabe989` 对应 activity/outbox 各 1 条。
  两组织 bootstrap correlation `e0a3d9df-4a84-43f2-8cc8-a7eb72e707a6`
  同样各有 audit/activity/outbox，bootstrap actor 保持 NULL，不冒充登录用户。

## 真实销售会话补记

- 用户自行完成销售登录后，组织选择只列出 A，没有 B。进入首页能看到 A 的测试线索。
- 线索公司名和联系人保持可见；来源、备注及全部活动正文显示保密，未出现文本审核入口。
  销售保留创建和推进操作；这不是成本报价样例的真实会话验收，不能从空白成本推导成本隔离通过。
- 销售通过页面依次执行 qualify/contact/respond/convert，最终 CONVERTED/v5，时间线 5 条。
  转换后的 company `38fcc498-9c40-4e96-835a-7d286b78ace7`、contact
  `653c6dcc-3182-4fa1-966f-5719263cc8f5`、opportunity
  `976e2b2e-552d-459d-b4b6-ae0226893c68` 已持久化。
- 只读数据库核对四步 actor 均为销售 `016ba854-6e06-4a5f-953a-9d32016bd67f`，
  每步 audit 对应同组织、同对象、同 correlation 的 activity/outbox 各 1 条。
  创建动作仍保留原经理 actor，不覆盖历史；销售刚写入的活动正文仍不可见。
- 在真实配置的 localhost:3300 独立发送不带 cookie/token 的合成请求：缺失回调状态、
  虚构 code/state 均 400 `LOGIN_CALLBACK_INVALID`；不可信 Origin 的 POST 为
  403 `ORIGIN_DENIED`；匿名业务读取为 401 `SESSION_EXPIRED`。
  未使用销售凭据，这证明边界拒绝，不证明已登录浏览器的全部 CSRF/会话撤销情形。
- 本轮未变更业务代码、身份授权或容器，也未将销售会话换为测试令牌。

## 本机联合恢复补记（2026-09-07）

用户已选择 CurrentUser 加密并完成恢复后的经理登录。恢复副本 A/B 选择、A原线索
CONVERTED/v5/五条历史及经理审核入口、B列表为空/原详情拒绝/无新增入口均已实际核对。
已切回原 Web/Logto并核验端口、健康及浏览器隔离，全部备份和数据卷保留。
详见 JOINT_LOCAL_RECOVERY.md；下方历史加密待选择/联合恢复未完成记录已被本次结果取代。
本次不关闭含成本商业单据的真实会话权限矩阵，也不宣称异机灾备或生产配置验收。

## 后续验收边界

### 恢复切回后的商业样例准备（2026-09-07）

经理在原服务 A 通过真实页面新建合成产品 `ac786864-44bc-466d-8df7-d46224d76983`，
SKU `LOCAL-ROLE-20260907-A`，名称“本机权限验收商品 A（非生产）”。表单提交标准成本
73.41 CNY，说明含相同合成成本及“未经审核不得开放”标记，页面确认产品已建立。
随后产品目录及详情可读；经理可见说明和审核入口。产品详情本身未展示独立标准成本栏，
不能把说明中的金额当作结构化成本读取验收；尚需销售真实会话及受保护响应检查。
未提交说明审核，未建立报价/订单或对外发送内容，没有改动原线索或已有商品。
经理已退出，原 Logto 登录页已交给用户切换 `trade_acceptance_sales`。
继续时先核对当前会话，再检查产品识别信息可见、保密正文和成本不可读；不要重复建商品。

### 含成本商品与报价的真实销售会话（2026-09-07）

用户确认销售再次登录。Edge业务页实际核对：组织选择只有A；商品名称/SKU/单位可见，
说明显示“待审核，当前不可见”，无产品创建、成本/供应商维护或文本审核入口。
新建合成询盘 `585aad47-0857-4f9b-b555-588fc78bc7b4`，参考号 `LOCAL-RFQ-20260907-A`，
关联原客户与商机。报价 `bdc024ff-2c12-4f66-baef-6bd68de8a61e` / `Q-2026-000001`：

- 销售真实页面创建V1：10 set ×120 CNY =1200；有效期2026-12-31，基准CNY汇率1。
  表单无内部成本输入，只由后端继承经理准备的商品成本。
- 销售改售价125生成V2，总额1250；V1变为SUPERSEDED，V2提交后INTERNAL_REVIEW。
  两版账本保留，页面无成本/毛利，来源询盘及商品快照说明均保密；无批准按钮。
- 原数据库只读核对：两版单位成本均73.4100、总成本734.1000，毛利分别465.9000、
  515.9000；两版原说明都与商品源完全相等。没有用空值覆盖源文或成本。
- 创建、修订、提交审计actor均为销售`016ba854-6e06-4a5f-953a-9d32016bd67f`；
  按同组织/报价/动作/correlation逐项核对，各有匹配activity及outbox各1条。
  创建报价的同correlation还包含商机推进证据，不能把不限定对象的总数2误判为重复。
- 这是实际浏览器操作加后台只读快照/审计证据，未抓取带真实会话的原始网络响应，
  不把页面隐藏单独当作服务端字段保护证明；服务边界另有已记录权限测试。
- 未批准、发送、接受或创建订单；未开放任何含成本说明。销售已退出，业务登录页
  交给用户用`trade_acceptance_manager`登录，下一步核对经理成本视图及审批。

### 同一报价的经理真实审批（2026-09-07）

用户完成经理业务登录，Edge页`1152814215`进入A。同一V2显示收入1250、成本734.10、
毛利515.90、毛利率41.27%，单件成本73.4100 CNY，原说明及审核入口可见。
页面执行“经理批准”成功，批准时间2026-09-07 16:52:26（本地）。数据库只读核验
approved_by为经理`2c02bec7-5da9-439b-a67c-49c7001948b0`，批准审计及匹配活动/outbox各1。
两版原金额不变，released_digest仍为空；业务批准没有自动开放保密正文。

随后尝试非生产样例“发送给客户”被工具安全审查拒绝，未执行；不得通过API或其他通道
绕过。只读核验V2仍INTERNAL_REVIEW、sent_at为空，没有quotation.sent审计。
源代码send仅走状态命令，worker默认处理只写消费回执，但这不替代该具体操作授权。
当前须用户明确批准此样例的模拟发送；尚未发送/接受/生成订单，不宣称商业主链完成。
经理会话保留在报价页，无需重复登录。不要把管理员控制台登录当作业务身份验收。

### 用户明确授权后的模拟发送（2026-09-07）

用户明确回复“执行模拟发送吧”后，重新打开原报价（旧标签已关闭），保留的经理会话有效。
Edge页`1152814226`核对Q-2026-000001/V2仍已批准后执行发送成功。
数据库只读结果：V2为SENT，sent_at为2026-09-07 08:57:49.47523 UTC；金额1250、
成本734.10、毛利515.90不变，V1仍SUPERSEDED，两个版本released_digest仍为空。
quotation.sent审计1条，actor为原经理，匹配quotation活动与outbox各1条。
当前worker任务文件SHA256与已检查本地文件一致（BC1DBAD6985F6C5AF0A1ABDE35D1445CB56C434003185AC30505FD565F4F65C2）；
该事件由默认处理器仅持久化消费回执，没有邮件发送实现。此结果是非生产状态模拟，
不代表真实客户收到报价。此补记取代上文“等待模拟发送授权”，没有开放保密说明，
也尚未记录客户接受或生成订单。继续验收不得重复发送或改写旧版本。

### 真实经理会话继续非生产订单与定金验收（2026-09-07）

经理会话在Edge`1152814236`继续同一合成报价，先登记明确标注“非生产验收、无真实客户
承诺或外部消息”的审阅依据，再记录模拟客户接受。V2为ACCEPTED，商机数据库状态WON。
从接受版本生成`SO-2026-000001` / `dec63e43-7896-413f-b0c0-b2b5939a60a3`，
总额1250 CNY、成本734.10、定金比例0.3000、定金375，到期日2026-09-07。
经理确认后进入DEPOSIT_PENDING，采购准备任务出现。原库只读核验订单绑定accepted_version_id，
订单行说明/单位成本与接受报价行一致，订单released_digest为空，没有继承任何文本开放。

真实页面生成两期应收：定金375于2026-09-07到期，尾款875于2026-09-14到期。
以`SIM-DEPOSIT-20260907-A`记录合成收款375，备注明确“非生产、无实际到账或转账”，
形成PAY-2026-000001；选择该收款和定金应收核销375后，页面定金结清、收款可用额0，
订单自动变为EXECUTING。没有实际银行操作，没有使用财务豁免。
首次只读应收查询使用错误列名kind失败；核对模型后改用installment_type，未重做商业写入。
采购、尾款、出运、文件及结案尚未完成；当前经理会话保留在此订单页，继续不得重复建单或收款。

### 同一订单的采购完成与出运上传配置（2026-09-07）

经理真实页面新增独立合成供应商4694dc37-eaa8-414c-8214-143fa6982c2b，未改原客户角色。
PO-2026-000001 / f8b592f3-039d-4e5f-abb6-4e9a2d4a63ca通过草稿、批准、发送记录、
供应商确认、模拟收货至CLOSED。确认号SIM-SUPPLIER-CONFIRM-20260907-A，交期Sep7。
源库只读确认10件订货/10件收货，单价73.41、总额734.10 CNY；六个采购动作审计各1条，
actor均为原经理。采购准备任务为DONE。没有真实供应商消息、物流或供应商付款。
首次审计查询使用不存在的actor_id失败；改为模型actor_user_id后只读核验通过，无业务重写。

SHP-2026-000001 / cb719b3e-9627-4d04-9716-61579b81dbe0已创建，原库为PLANNING，
页面10件货物，缺商业发票和装箱单。检查发现验收API继承localhost:9000签名地址，
但验收MinIO未映射端口，该端口属于原主环境。新增acceptance-storage.compose.yml，
仅向127.0.0.1开放9300并同步API公开签名端点，内部仍minio:9000。
确认端口空闲及项目/原卷后，仅重建验收API/MinIO，保留trade-fresh-acceptance_minio-data。
沿用内存中的既有身份配置，不输出密钥、不重建Web、不清理恢复容器或卷。
Compose config --quiet与格式检查通过，API/MinIO健康，存储和Web健康HTTP200。
code-simplifier复核保持为独立最小配置覆盖，不改业务代码或基本启动隔离规则。

fixtures/SIM-INVOICE-20260907-A.txt及SIM-PACKING-20260907-A.txt仅供非生产上传验收，
不含内部成本/利润，不代表法定文件。点击文件选择后未找到独立可定位的文件对话框；
尝试读取整个Edge窗口被安全检查拒绝（包含无关私人标签），未绕过。
当前交给用户手动选择并上传两份文件；未声称上传、扫描或固定对象版本已通过。
该补记取代前文“采购尚未完成”，尾款、出运、文件及订单结案仍待继续。

### 上传到达但商业发票内容误选（2026-09-07）

后续只读复核发现出运关联已有两份文件，COMMERCIAL_INVOICE和PACKING_LIST均AVAILABLE，
expected/actual大小和SHA256一致，均固定了storage_version_id。内置浏览器页3也显示两份可用。
这证明上传配置修正后的两次实际上传完成；扫描仍只是已披露的框架占位，不代表恶意文件检测。

内容验收发现两份file_name均为SIM-PACKING-20260907-A.txt，actual_sha256均为
81b31c76946844105cbf947ffa718894c619b7be97c1c5f5e4ee4013829b90b8，
与本地模拟装箱单一致。正确商业发票的SHA256应为
1c0bfedb1bf7ac4ea26165f98b9358c18c52bd83b7ba249a2728f2e55ff7c9d8。
因此不能仅因文件类型齐全而宣称证据正确；尚未推进出运，也未开放附件。
已请用户对商业发票执行“替换文件”，保留原版本，不删除或覆盖旧对象。
再次只读复核仍只有各自V1，尚无替换版本。原先“附件数0”的等待已由此新证据取代。

### 发票替换及真实会话模拟交付（2026-09-07）

用户回复“已替换”后，只读核验商业发票V2和V3均为正确SIM-INVOICE文件，
SHA256与上述正确样例一致，AVAILABLE、大小一致且固定存储版本；旧误选V1保留。
装箱单V1未变化。当前页面为发票V3；不推断V2/V3产生原因或声称是幂等重试。
文件列表已显示可用，但出运缺件提示仍显示缺商业发票；刷新后齐套，记录为
异步文件完成后的清单刷新问题，尚未修复。页面“通过扫描”仍不代表生产恶意文件检测。

当前内置浏览器页3真实业务会话，以SIM-BOOKING-20260907-A记录模拟订舱，
依次执行齐备、报关中、离港、在途、到港、交付。页面DELIVERED，节点变只读，
上传/替换入口消失，历史下载保留。全部是本机非生产记录，不联系货代或海关。
尾款结清、订单完成及后续完整验收尚未完成，不以物流终态代替商业结案。

### 真实会话尾款核销及订单完成（2026-09-07）

当前内置浏览器页3进入同一SO-2026-000001：尾款875未结、采购任务DONE。
先在财务豁免留空时执行结案检查，页面拒绝并提示所有应收必须结清或经授权豁免，
未据此重试绕过。随后记录合成尾款875 CNY，日期Sep7，流水号SIM-BALANCE-20260907-A，
备注明确无实际银行到账或转账。生成PAY-2026-000002，选择该笔收款与尾款应收，
准确核销875，页面尾款PAID、收款可用0。再次无豁免结案成功，页面COMPLETED并保留时间线。

源库只读核验COMPLETED，总额1250、成本快照734.10、预计毛利515.90未变；
DEPOSIT375/净核销375及BALANCE875/净核销875均PAID。完成审计actor为原经理，
同组织/对象/correlation的sales_order.completed活动及outbox各1条。
这是本机合成订单主链，不是实际银行收款或真实利润；仍未关闭报关退税人工跟踪、
完整真实角色矩阵、已发现的清单刷新问题或整个V1总验收。

### 完成订单后的人工报关样例（2026-09-07）

真实业务会话首页不再列出原订单应收、定金、采购、出货待办，各对应队列为空。
通过首页报关入口创建CUS-2026-000001 / 6c7fb5a7-fa1f-45da-a4fb-26ca07e1f069，
关联原出运，合成人工金额1250 CNY、跟进Sep7；备注明确非生产、不构成法定申报依据、
未向海关提交。页面由草稿经开始准备资料进入待补文件。
本案件清单独立于出运文件，仍缺商业发票/装箱单；填写明确负向验收原因尝试确认就绪，
页面拒绝并提示必需文件未齐备或扫描未完成，状态没有推进。随后关闭操作表单，保留案件页。
当前需给案件上传正确模拟商业发票与装箱单，不得通过清空必需清单或更改文件元数据绕过。
本次记录为实际页面观察，尚未追加该案件的数据库审计对照或模拟申报/清关/退税后续。

### 报关附件补齐及就绪（2026-09-07）

后续商业发票到达：CUS-2026-000001关联的发票及装箱单文件名/实际SHA256分别
与前述正确本地样例一致；expected/actual大小及摘要相符，storage_version_id均固定。
真实页面“缺少文件：无”。填写明确非生产、仅软件资料齐套而非满足法定要求的理由，
执行确认资料就绪成功，页面为READY并追加ready时间线。未执行政府提交。
出运清单刷新修复已通过完整浏览器31项及新增免刷新断言的商业主链1项；
尚未替换正在运行的验收Web，不将本次报关可用状态归因于未部署的出运代码修复。

### 模拟清关与退税案件建立（2026-09-07）

真实页面在CUS-2026-000001资料就绪后记录模拟申报：Sep7，回执
SIM-CUSTOMS-SUBMIT-20260907-A，原因明确未向海关/外部机构实际提交。
随后模拟清关Sep7，页面CLEARED，状态命令、跟进调整及上传/替换入口消失，
已有附件下载与历史时间线保留。首次退税导航点击遇到元素脱离，重新观察后点击成功，
未重做清关命令。此处为页面验收，数据库证据逐项对照仍需追加。

创建REF-2026-000001 / e9d8a379-cfa0-49ec-894f-2109fdc556e2，绑定原报关，
任意合成预计金额100 CNY、跟进Sep7，备注明确不是法定应退额且无真实申报/到账。
页面NOT_READY，实收0、差额100，缺提单和商业发票。准备SIM-BOL-20260907-A.txt，
明确不可用于运输、物权、海关、税务或付款；等待该案件的两份模拟文件上传。
不通过取消清单或借用报关文件的未关联元数据来推进状态。

### 报关／退税原库证据对照（2026-09-07）

组织限定只读事务确认原报关CLEARED，1250 CNY，模拟回执号及提交/清关日期均Sep7；
关联退税REF-2026-000001为NOT_READY，预计100、实收0。报关创建、请求资料、就绪、
模拟提交、清关及退税创建共六条审计，actor均为原经理；每条同组织/对象/correlation
匹配活动1及outbox1。未出现重复就绪审计，先前缺件拒绝没有生成成功证据。
当前退税document_links查询0行，未上传案件附件，不能继续确认齐套或登记退款。
此核对补齐前文“数据库证据待追加”，不构成全部V1验收。

### 既有身份拒绝回归明细

完整读取相关测试后执行 auth_unit、oidc_algorithms、tenant_isolation、
identity_administration、identity_acceptance_fixture 五组，102 项通过，44.49 秒，退出 0。
有两条警告：依赖的 TestClient 弃用提示，以及 pytest 缓存目录访问被拒绝；
后者影响缓存写入，不是测试数据库或业务写入失败。没有修改既有目录权限。

| 要求                                                   | 当前证据                                                                          | 证据边界                                                   |
| ------------------------------------------------------ | --------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| 错误 issuer/audience、过期、错误签名/key、错误曲线拒绝 | oidc_algorithms 使用新建 RSA/P-384 密钥和真实 PyJWT 验签，生产 LogtoTokenVerifier | JWKS 网络读取被替换为测试公钥，不是修改真实 Logto 发行配置 |
| 无组织、无成员、禁用用户/成员/组织拒绝                 | tenant_isolation 逐项核对稳定错误码                                               | 真实 PostgreSQL，隔离测试 issuer；没有停用用户当前会话     |
| 组织选择与令牌组织约束、主键/列表/计数隔离             | tenant_isolation、非对称算法集成测试及上文真实 A/B 页面观察                       | 各自证明其覆盖端点，不能泛化为全部业务端点的真实会话矩阵   |
| 成员撤权即时复核、禁止旧管理员上下文、最后管理员竞争   | identity_administration                                                           | 临时测试数据库，未给本机经理额外 ADMIN 权限                |
| 初始化不覆盖、证据失败全部回滚                         | identity_acceptance_fixture                                                       | 在临时测试库调用 fixture；未重跑已初始化真实验收库         |

### 联合恢复源盘点（只读）

当前 `trade-fresh-acceptance-postgres-1` 同时保留 `trade_fresh_acceptance`（真实登录样例）
和 `trade_workbench_e2e`（完整商业样例）。联合恢复必须同时覆盖两者与原始 MinIO 版本，
不能仅备份有真实用户但没有完整商业链的数据库。
初始化 Logto 源卷为 `trade-workbench_logto-postgres-data`；没有读取密码或应用密钥字段。
旧 commercial-postgres-restored 与 commercial-minio-restored 容器仍停止，必须保留原卷。
尚待用户选择含凭据备份的加密保护方式；本次仅盘点，没有创建登录库转储、读取私钥、
停止身份服务或创建恢复目标。

- 本轮完整前端 41 文件 / 228 项通过；线索浏览器专项 6 项通过（39.9 秒），
  包含转换主链和 375/768/1024/1440 宽度检查。浏览器专项使用隔离测试身份，
  不替代上面的真实 Logto 会话。独立转换权限补充后完整浏览器再跑 31 项通过
  （3.0 分钟，退出 0），包括报价至收款、完成、退税主链与角色切换。
- 最终镜像生产构建通过，实际 Web healthy；最终 TypeScript/ESLint、格式、
  生成客户端无漂移、bootstrap Ruff 与 mypy（211 文件）通过。
  最初未提权的 Docker 健康检查被沙箱拒绝，随后获准只读检查得到 healthy。
- 尚未关闭：真实会话下有成本样例的商业权限、完整身份矩阵；初始化后的 Logto
  与商业库/版本化对象联合恢复；生产模型、恶意文件扫描和部署配置验收。
  旧商业恢复备份保持不变，不得覆盖现有验收数据以伪造空环境结果。
