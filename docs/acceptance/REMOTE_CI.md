# GitHub 上传与远程验收

## 上传恢复与第四次运行（2026-09-09）

普通push已成功：3314428..de8add8，远程main现为
de8add878148c37658d12d9fca39ed67e62f1753。未改变网络代理、证书校验或仓库权限。
[运行34316018142](https://github.com/zhangjiabao271-web/foreign-trade/actions/runs/34316018142)
已由GitHub接口确认in_progress，精确对应该提交。quality job102352290878已完成
客户端/漂移/格式/lint/类型检查；pnpm test已success，浏览器主链运行中，
尚无完整成功结论，具体测试数量待终态日志核对。
container-smoke job102352290685已success，原始日志确认20260908_0035(head)、
Web/API ready及三项依赖true、Worker pong和1 node online。
此运行含合同、身份、CRM、公司、产品和worker补充，不含随后本机新增的Work测试。
下节上传网络失败是已结束的历史阻碍，不再当作当前等待用户操作的理由。

## 本机新提交待上传：网络阻碍（2026-09-09）

本机af0d5029723ff881ecce5d4038d4d4814b3483a8包含worker/company/catalog/
identity/CRM补充和只读GET单次连接重试。浏览器66338最终32通过/3.3分钟；
专用trade_workbench_e2e数据库数量0，3100/8010无监听，测试凭据和两份进程记录不存在。
最终三文件扫描无告警；原业务环境未部署或修改。

三次普通非强制push均终止：首次连接重置，后两次github.com:443连接失败，
其中最后一次只对该命令使用HTTP/1.1，没有修改持久配置或关闭证书校验。
首次失败后GitHub只读接口确认远程main仍33144281a8a952e13a13440a905020aa534cb8ae。
尚未确认新提交上传，不能声称新CI已启动。后续先核对远程引用再继续普通push，
不要重建仓库、提取令牌或重复已完成的业务验收。

## 第三次运行终态：只读请求连接中断（2026-09-09）

34312341146已结束，对应33144281a8a952e13a13440a905020aa534cb8ae。
quality原始日志：Web267通过；API/Worker1868通过、6跳过、1警告，1490.32秒。
浏览器17通过、1失败、14未执行，2.1分钟；发布构建与最终配置检查跳过。
失败是quotation-lifecycle.spec.ts:783的订单正文审核GET发生read ECONNRESET，
没有收到HTTP响应；不是成本断言失败。缺少该时刻服务端诊断，底层重置原因未证实。
仅对此只读GET设置Playwright maxRetries:1；已安装类型文档明确它只重试ECONNRESET，
不重试HTTP错误。业务POST、权限断言、等待时间及全测试retries:0保持不变。
本机及新提交远程复验结果须另记，不能据此声称故障已解决。

本机复验session66338已退出0：32通过，3.3分钟，包含完整主链及两个成本角色切换。
日志tmp/e2e-read-reset-20260909.log；last-run为passed、failedTests为空。
类型、目标ESLint/Prettier、补充Python测试Ruff及diff检查通过；此次未重跑完整API。
选定六文件上传快照Gitleaks无告警，不包含浏览器trace或环境凭据。
本机通过不证明远程底层连接问题已消失，新提交CI仍必须独立取得终态。

container-smoke原始日志独立确认20260908_0035(head)、Web/API ready、
PostgreSQL/Redis/MinIO探测为true、Worker pong及1 node online。
此次运行不包含后续worker/company/catalog/identity/CRM补充，不能套用其通过结果。

## 第三次运行检查点（2026-09-09 12:51 CST）

已上传至私有main：33144281a8a952e13a13440a905020aa534cb8ae。
本轮包含费用财务/租户、AI工具、全业务认证入口补充及角色响应关联修正。
[运行34312341146](https://github.com/zhangjiabao271-web/foreign-trade/actions/runs/34312341146)
确实对应该提交，push触发。container-smoke job102341398607已success；quality
job102341398443已通过客户端/漂移/格式/lint/类型门槛，pnpm test仍运行。
浏览器、发布构建与最终配置尚无通过结论；不轮询旧的两次失败为运行中。
上传前本次五文件Gitleaks扫描无告警；先前认证测试扫描的合成密钥提示仍按已审阅
误报保留，不把整个历史描述为零告警。浏览器23350结束后专用数据库数量0，
两份测试进程记录、登录文件均不存在，3100/8010无监听。本轮不部署业务环境。
随后新增的worker AI租户入口测试不属于该提交，必须独立记录验证。

## 第二次运行终态：角色响应关联问题（2026-09-09）

34309375656现已结束：container-smoke成功；quality失败于浏览器第31项报价角色切换。
其完整日志确认Web262通过；API/Worker1482通过、6跳过、1既有警告，1387.85秒。
浏览器31通过、1失败，5.5分钟；完整订单主链、订单成本角色切换和出运来源均通过。
发布构建和最终配置步骤跳过，不能称为完整CI成功。不得再轮询此运行为运行中。

失败明确为经理预期total_cost="224.0000"而收到null，并非观察到低角色获得成本。
本次fixture未出现五分钟过期问题。源码显示beforeEach以销售初始化页面，报价检查
在goto后立即切经理，而waitForResponse原来仅匹配GET/地址/200，不关联请求身份；
因此打开页面时的销售请求可以被当作经理响应。没有保留远程trace下载产物，故不
将具体失败网络请求的身份猜测写为已证实的服务端原因。

修复两个角色检查共用的响应匹配：同时要求实际请求Bearer与组织头对应目标角色。
新增五项单元测试明确排除先到达的旧销售响应、错误组织/方法/状态/对象；全部通过。
原成本断言、UI检查和等待时间不变，真实认证/权限不改。单元文件放在lib，避免被
Playwright的e2e目录误收集。最终位置的五项单元与类型检查通过，目标lint/格式通过。
完整浏览器复验23350正常退出0，32通过/4.0分钟；包括报价及订单角色切换。
日志tmp/e2e-role-response-20260909.log，apps/web/test-results/.last-run.json为passed，
failedTests为空。预检专用库不存在、3100/8010无监听或测试进程、测试凭据不存在。
结束后3100/8010无监听、测试凭据清理已确认。不触碰3300业务环境；修复及近期补充仍未推送。

## 第二次运行已启动（2026-09-09 12:02 CST）

已将并发补充测试02c437f及测试令牌修复30f931b推送到私有仓库main。
远程确认新运行 [34309375656](https://github.com/zhangjiabao271-web/foreign-trade/actions/runs/34309375656)
对应完整提交30f931bf8931f0a44777d64a60cfe850bfd77199，push触发，当前in_progress；
尚无成功结论。上传前五文件最终Gitleaks扫描无告警，Ruff/格式及diff检查通过。
本机32项浏览器验收通过后独立确认专用库不存在、3100/8010无监听、测试凭据和
进程记录已清理。未部署业务服务，也未创建持续后台自动化。

随后实时读取确认container-smoke job102332660416成功。其原始日志实际输出
20260908_0035 (head)、Web/API健康检查成功和Worker pong/1 node online。
quality job102332660545已通过生成/漂移/格式/lint/typecheck和MinIO准备，
正在pnpm test；浏览器、发布构建和最终配置步骤仍未完成。
本机新增费用财务边界测试尚未上传，不属于此运行覆盖范围。

后续实时步骤状态确认pnpm test及Chromium安装已success，当前正在完整test:e2e。
暂未取得该job的归档日志，因此不在此推断实际测试数量；最终构建和配置仍需等终态。

## 首次运行终态与浏览器测试令牌修正（2026-09-09）

运行34295330627已结束，quality失败于浏览器测试，不得再轮询为运行中。
原始日志确认Web45文件/262测试通过；API/Worker1475通过、6跳过、1既有警告，
1397.03秒。6跳过为5项真实付费模型选择退出及1项Windows专用进程所有权测试；
不是少执行一项业务测试。浏览器29通过、2失败、1因串行组失败未执行，5.7分钟。
发布构建与最后配置检查未执行。container-smoke独立成功，保留前述证据。

失败时序：测试fixture于00:58:21UTC就绪；01:03:21UTC开始/me/context、订单与采购
读取401。原fixture四角色令牌均沿用LocalTestTokenIssuer的5分钟默认，故最后的
角色切换和出运来源测试在过期后失败，而非仅慢断言。两处具体失败为
quotation-lifecycle.spec.ts的order snapshot roles等待200超时，及shipment-sources
找不到授权后的终结订单提示。未降低权限、禁用过期校验或提高测试等待时间。

仅e2e_fixture.py的四角色测试令牌显式改为30分钟，通过同一小型签发函数统一；
一般测试发行方仍默认5分钟，生产Logto验证未改。新四角色时钟测试验证第6分钟
仍有效、30分钟边界拒绝，并检查一般默认未变；连同auth_unit/OIDC算法测试43通过，
4.72秒，1既有httpx警告。初始行宽告警经格式化后Ruff复核通过。
本机首次复验session19306在中断后失去进程句柄，未取得完整终态，不能算通过。
其失败trace显示01:53:52UTC采购发送及原样重试均200，随后采购/订单列表500；
后续出运测试的权限上下文请求未完成。它发生在签发后两分钟内，不能归因于令牌
过期。未保存该次服务端异常日志，故具体500原因仍未确定；不据此修改业务权限。
失败附件保留于本机已忽略tmp/e2e-interrupted-20260909-0952，不上传其中的会话信息。
重新只读核实3100/8010无监听、无测试进程、专用trade_workbench_e2e库零活动连接后，
启动一次完整复验session53837，完整输出保存到tmp/e2e-rerun-20260909.log。
仅重建该专用测试库，不触碰3300及本机验收/恢复卷。
session53837随后正常退出0：32项全部通过，3.7分钟；.last-run.json为passed且
failedTests为空，测试登录文件及进程所有权记录已清理。之前两处本机失败没有重现，
但缺失的旧服务端异常日志仍不能由这次成功补证为已知根因。
修正尚未推送，首次失败不被抹去，后续须按新提交核对远程结果。

## 首次远程运行检查点（2026-09-09）

已成功上传提交 `8b8d6bc8c3e288d64b80059048073cf25b339306` 到原空仓库main，
远程运行 [34295330627](https://github.com/zhangjiabao271-web/foreign-trade/actions/runs/34295330627)
已启动，不再是“未配置仓库/未执行远程CI”。本次读取对应job状态与原始日志：

- container-smoke job102290598320：success；Ubuntu24.04全新检出该提交，构建/启动
  完整Compose，Alembic实际输出 `20260908_0035 (head)`，Web/API探测成功，Worker
  实际返回pong。清理仅发生在GitHub临时runner的测试卷，本机业务及恢复卷未动。
- quality job102290598078：仍in_progress；依赖安装、客户端生成及三项漂移检查、
  格式、lint、类型检查和MinIO启动均success；当前 `pnpm test` 尚无终态。
  浏览器主链、发布构建及最终配置检查尚未执行，不宣称整个运行通过。

启动日志最初有本地API标签的远程pull失败提示，随后源码构建及所有冒烟步骤成功；
不能把该中间提示当作最终失败，也不隐藏它。GitHub另有旧action声明Node20的弃用
提示，runner实际强制使用Node24；不因此放宽安全版本策略。

曾尝试创建持续后台跟进，但权限审核因缺乏明确的长期自动化授权拒绝；未创建，
不得把后续目标自动续行当作该授权。当前交互任务内检查既有运行仍可继续。

## 上传前检查（2026-09-09）

用户明确批准向 `zhangjiabao271-web/foreign-trade` 上传已检查的项目源码及测试配置，
并运行 GitHub Actions。实时连接账号一致；仓库为 private，具有 push 权限；
本机认证的 `git ls-remote` 成功且没有任何引用，首次上传不覆盖远程历史。

上传前固定快照包含728个受版本管理文件；本记录是随后新增的验收说明。
三个可达历史提交及当前快照均检查；`.env`、备份、临时文件、第三方内部PDF/DOCX
未在受版本管理的文件或历史路径中发现。保留本机原文件，不上传数据库或对象存储。
本机模拟验收ID、公开客户端ID、镜像摘要和证据说明不是登录凭据，予以保留。

使用官方MIT许可Gitleaks8.30.1，Windows压缩包SHA-256与官方校验清单一致：
`d29144deff3a68aa93ced33dddf84b7fdc26070add4aa0f4513094c8332afc4e`。
程序、固定快照及脱敏扫描报告仅留在已忽略的 `tmp/upload-audit-20260909`。
历史扫描3次提交约6.26MB，当前快照约6.07MB，两者均报告7项提示，退出码1；
逐项检查确认以下非真实密钥，不将原始退出码描述成“扫描器零告警”：

- auth/tokens.py：公开的EC曲线类型名称。
- 两个API测试文件：明确命名的合成测试密钥字符串。
- release-0035配置及settlement部署脚本：本机镜像标签。
- V1_STATUS两处：镜像SHA-256摘要。

补充常见令牌/私钥模式及凭据赋值检查未发现实际密钥。规则扫描和人工检查不构成
绝对无泄漏保证；不得因此将运行环境、备份或未检查文件加入上传。

## 验收边界

首次上传将触发main的quality与container-smoke任务。测试使用隔离合成数据库/存储，
不配置真实模型密钥、不运行付费模型验收、不发布应用或容器镜像。
本记录建立时远程任务尚未执行；必须按实际提交和任务结果补记，不预先宣称通过。
远程CI通过也不能代替完整指南逐项审计、实际登录及另行约定的生产部署/异机灾备。
