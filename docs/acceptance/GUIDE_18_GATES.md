# 实施指南第 18 节：总验收证据索引

核对日期：2026-09-06。此表不是验收证书，也不替代指南其他章节、ADR 或用户确认的权限规则。
阶段测试通过不能自动使总体验收通过。持续更新的原始运行结果见 V1_STATUS.md；
未完成的原文权限覆盖见 TEXT_POLICY_SCOPE.md，其他实现和外部条件见 REMAINING_SCOPE.md。

## 十项演示要求

### 最新闭环补记（2026-09-07 19:15）

此补记取代下方表格18.5–18.8的旧缺口：原真实经理会话中出运已DELIVERED，
商业发票/装箱单可用；定金375与尾款875均已核销，订单COMPLETED且无财务豁免；
报关CLEARED，退税REFUNDED。退税合成预计100、实收90、差额10 CNY，文件齐套。
数据库按原组织限定核对退税六动作经理审计，各匹配活动1、outbox1；
终态关闭业务修改入口，保留历史和独立审核。详见REAL_IDENTITY.md最新补记。
均为非生产模拟，无实际发送、物流、银行到账或政府申报。
真实会话有成本样例的完整角色/内容审核矩阵、完整指南与ADR逐项收口仍待完成。
不得将下文旧上传交接当作要求用户再次上传，也不得据此宣布全部V1通过。

### 当前检查点（2026-09-07，取代下方同项旧缺口）

本轮重新核对REAL_IDENTITY、V1_STATUS及JOINT_LOCAL_RECOVERY记录；下方旧表保留作历史，
不可把已经完成的登录和联合恢复继续当作待用户授权事项，也不可将本汇总当作重新跑测试。

| 项目      | 最新证据                                                                                     | 当前边界                                                 |
| --------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| 18.1      | 真实经理A/B隔离、真实销售A线索转换；身份拒绝五组102项                                        | 不泛化为全部业务真实身份矩阵                             |
| 18.2      | 真实销售创建V1/修订V2/提交；经理批准并经用户明确授权模拟发送，原库审计核对                   | 无真实邮件渠道或外部送达                                 |
| 18.3      | 同一V2模拟接受、商机WON、SO-2026-000001快照与原文/成本一致                                   | 保密正文未开放                                           |
| 18.4      | PO-2026-000001已收10件并CLOSED，734.10 CNY，六动作经理审计各1；采购准备DONE                  | 无真实采购通信、物流或供应商付款                         |
| 18.5      | SHP-2026-000001仍PLANNING；上传端点误指主环境已修复且健康通过                                | 真实会话附件上传、出运与交付仍未完成                     |
| 18.6      | 合成定金375已核销，订单EXECUTING，尾款875待处理                                              | 同一真实会话样例未结清；三币种证据仍来自独立测试身份主链 |
| 18.7–18.8 | 独立商业测试主链与恢复记录仍可引用其限定范围                                                 | 当前真实会话订单未完成，未推进报关退税                   |
| 18.9      | 下方持久化回执复核及21项专项已有记录                                                         | 本轮未重新制造broker故障                                 |
| 18.10     | 0033后端976、浏览器31等回归记录；初始化Logto/两商业库/原对象版本联合恢复及真实恢复登录已通过 | 不是全部门槛关闭；不含异机灾备、生产模型/扫描/部署验收   |

当前上传交接：浏览器原出运标签已关闭；文件选择无法安全隔离到独立对话框，
整个Edge窗口读取因无关私人标签被拒绝。不得绕过，需用户选择两份已准备的模拟附件。
后续仍需逐项对照完整指南及ADR，不能由本表推导未覆盖条款通过。

2026-09-07 加密联合恢复补记：按用户选择使用本机 CurrentUser DPAPI，三套库逐表指纹、
原角色授权及8个原始文件版本恢复验证通过；恢复后销售会话与保密线索正常。
新的经理登录已通过：A原线索及审核入口恢复，B列表为空、精确详情拒绝且无新增入口。
已切回原 Web/Logto，端口归属和健康200核验通过，全部卷保留，详见 JOINT_LOCAL_RECOVERY.md。
以下较早记录中的“联合恢复待完成”已被本补记取代，真实商业权限矩阵仍需完成。
18.10 不因“数据库恢复通过”而自动全部关闭，也不代表异机灾备或生产部署已验收。

2026-09-07 销售真实会话补记：组织选择仅 A，线索业务识别字段可见、未审核正文保密，
正常四步推进至转换，客户/联系人/商机及销售操作者三类证据已核实。真实配置 Web
另拒绝缺失/虚构回调状态、跨站写入与匿名读取。详见 REAL_IDENTITY.md 的测试边界；
尚不关闭有成本样例的真实权限矩阵与初始化 Logto 联合恢复。

2026-09-07 真实身份补记：经理经本机 Logto 登录，显式初始化后可选择 A/B；
在 A 新建线索，切 B 列表为空且精确详情不可读。发现并修复只读创建入口，
保留会话更新验收 Web 后复验通过。见 REAL_IDENTITY.md；这仅部分推进18.1，
不替代真实销售、完整身份拒绝矩阵和18.10初始化身份联合恢复。

2026-09-07 最终本地回归补记：0033完整API/Worker单次976项通过（1210.60秒），
随后完整浏览器31项通过（2.8分钟），两者均退出0。覆盖最终AI审核组织限定查询；
不再仅依赖0032的942项与分批专项相加。静态、生成客户端、格式检查通过。
这些结果不关闭真实登录或初始化身份恢复门槛，也不等同于生产模型/扫描配置验收。

2026-09-07 AI 更新：用户已确认主动提交的私有草稿审核边界，ADR-026/0033已实现并通过
专项、前端及浏览器主链验证，详见 AI_DISCLOSURE.md。原先等待私有草稿授权的门槛不再适用；
原始输入/工具/审批副本仍保持保密，不应把候选开放误当作全量原文开放。真实登录和
初始化后的身份恢复等总体验收条件仍未关闭。

2026-09-07 更新：完整商业 PostgreSQL + 版本化 MinIO 恢复已通过，45 表、8 个原始对象版本、
结清订单和权限隔离均一致，详见 COMMERCIAL_RECOVERY.md。下表 18.10 的旧恢复缺口由此部分关闭；
初始化后的 Logto 联合恢复和真实登录仍未通过，因此总验收结论不变。

| 指南要求                                         | 已定位的证据与边界                                                                                                                                                                           | 关闭总验收门槛前仍需证明                                                                                                                               |
| ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 18.1 两组织隔离的线索转换                        | lead-conversion.spec.ts 实际页面转换；test_crm_vertical_slice.py 与 test_tenant_isolation.py 验证组织边界；CRM 原文权限另有 0026 测试                                                        | 真实登录用户关联到本地成员后，复现组织选择和越权拒绝；不可用测试令牌替代                                                                               |
| 18.2 V1/V2 报价、销售提交、经理批准发送          | quotation-lifecycle.spec.ts 覆盖页面报价流程；版本和重试专项保留证据；0029 原文独立审核已通过 28 项浏览器及后端分批验证，详见 V1_STATUS                                                      | 关闭其他相关写入重试缺口后执行最终跨模块主链；不能把局部原文保护当作全系统完成                                                                         |
| 18.3 接受 V2、商机赢单、稳定订单                 | 同一浏览器主链；报价接受、商机终态竞争、订单成本保护专项；0029 证明原文复制保持不变且源审核不传递到报价、订单或合同                                                                          | 完成剩余采购和 AI 等权限切片后执行最终跨模块验收；不能由产品原文审核推导订单快照已审核                                                                 |
| 18.4 确认订单、采购任务/订单和承诺成本           | order_procurement、purchase_receiving、purchase_changes、purchase_cost_permissions 相关测试；0030 采购原文和历史独立审核通过 27 项专项与 28 项浏览器验证，供应商确认号及订舱号按识别字段保留 | 最终跨模块验收须复现审核不开放结构化成本、源审核不传递到历史；履约引用字段的代码分类不替代状态与并发验收                                               |
| 18.5 出货、文件、出运和交付                      | 主链实际上传 MinIO 文件并完成交付；document_storage、document_versions、document_review、shipping_progress 测试                                                                              | 最终运行全链检查；扫描框架占位必须明确，不能声称已经具备生产恶意文件检测                                                                               |
| 18.6 定金尾款、付款及准确核销                    | 主链 EUR 销售/付款、USD 基础快照、CNY 采购；finance_rules、finance_vertical_slice、payment_navigation；0028 收款备注独立审核，13 项专项及 28 项浏览器通过                                    | 最终跨模块回归与角色矩阵；收款备注审核不开放其他商业正文，不把销售收入误判为内部成本                                                                   |
| 18.7 满足条件完成订单及追溯                      | 主链任务、文件、交付、核销后完成；事务审计和 outbox 测试                                                                                                                                     | 最终故障注入和完整时间线证据对照；权限审核是独立事件，不得改变业务完成条件                                                                             |
| 18.8 人工报关退税及缺失文件/下一动作             | 主链从清关走到实际收到部分最终退款；export_vertical_slice；0027 原文保护和独立审核；该版本后端全量 597 项通过                                                                                | 保持最终全链证据一致；不把人工记录说成已向政府提交或计算法定退税权益                                                                                   |
| 18.9 Redis/Worker 重启、无丢失及重复消费无副作用 | test_platform_transactions.py 验证事务可见性、并发领取、确切消费者回执、重投和迟到重复；test_tasks.py 验证组织传播。V1_STATUS 另记录过隔离 Redis/Worker 实验                                 | 单元/集成的 RecordingDispatcher 不是实际 broker 重启；总验收引用历史实验时须核验保留的持久化回执和业务副作用，必要时在独立环境重跑，禁止清空正式 Redis |
| 18.10 空环境、迁移、测试、客户端、浏览器及恢复   | 商业分页0032全量942通过；后续来源直达另有专项2及关联65、前端219、浏览器31、静态/客户端及隔离构建运行通过；不是单次944项全量，见V1_STATUS                                                     | 初始化后的 Logto 与完整商业 PostgreSQL + 版本化对象存储联合恢复尚未通过；旧合成文件实验和空库迁移都不能替代该要求                                      |

## 18.9 持久化证据复核（2026-09-07）

只启动已保留的 `trade-recovery-acceptance` PostgreSQL，确认项目标签后执行只读事务。
按组织、任务和事件关联核对：`rehearsal.restart` 为 PUBLISHED、发布次数1、目标消费者回执1；
`rehearsal.broker-loss` 为 PUBLISHED、发布次数2、目标消费者回执1。
两者 `result_reference` 均准确指向对应事件。任务状态仍为 PENDING：该示例消费者只写事件引用，
不把样例任务标记为业务完成；这不是未投递，也不应据此声称真实业务任务已完成。

本次未启动消费者或 Redis、未重投或清空队列；检查后数据库已停止，卷保留。
它确认历史故障演练回执和副作用仍存在，不是新的重启/丢失演练。当前代码的
`test_platform_transactions.py` 与 `test_tasks.py` 另行重跑21项通过（11.91秒），
覆盖事务、消费者幂等、恢复条件及组织传播；测试替身部分仍不是实际 broker 故障证据。

## 性能验收范围

已直接核对 test_acceptance_performance.py 与 test-results/local-api-performance.json。
2026-09-06 23:46:41 UTC 样本为进程内 ASGI + 真实本机 PostgreSQL，单并发；
两组织各 1000 条线索、30 条出货、1 笔确认订单，每个操作预热 3 次、测量 60 次。

| 操作         | p95 毫秒 | 指南初始门槛 |
| ------------ | -------: | -----------: |
| 线索读取     |    9.018 |        < 500 |
| 首页线索队列 |   10.591 |        < 500 |
| 首页出货队列 |   11.650 |        < 500 |
| 创建线索     |   14.176 |        < 800 |
| 异步任务受理 |   26.304 |       < 1000 |

首页出货清单取 1 条和 20 条均为 3 次 SELECT，证明该路径查询数量不随页面行数线性增长。
这些样本不覆盖所有普通接口，不含网络/TLS、Logto/JWKS 网络、浏览器渲染或模型执行。
报告由测试重新生成，后续引用应核对 measured_at 和测试范围，不能混用为生产 SLO。

## 发布判断

当前结论：尚未完成总验收。真实会话模拟主链已推进到退税结案，完整权限证据仍需完成；
本机真实登录和联合恢复已由上述新记录推进，不再使用旧结论要求用户重复初始化。
生产服务商、区域、模型与扫描、密钥及异机备份另需配置。不得把本表中的测试文件名、代码存在、
旧运行成功或“未发现失败”作为所有要求已通过的证明。
