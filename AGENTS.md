# 外贸工作台施工规则

每个实现任务开始前，必须完整阅读 `docs/IMPLEMENTATION_GUIDE.md`、本文件和任务涉及模块的 README。

执行优先级：当前用户指令 > 已接受 ADR > 实施指南 > 模块 README > 代码注释。遇到冲突必须明确记录；涉及不可逆实现时先停止受影响部分。

## 不可破坏的基线

- 先打通一笔可盈利订单，再扩展功能面。
- PostgreSQL 是唯一业务事实源；Redis、Celery、MinIO 元数据缓存和 AI 都不是事实源。
- 所有业务数据绑定 `organization_id`，主键查询也必须限定组织。
- 状态变化通过后端命令和领域服务完成，不允许客户端直接写 `status`。
- 金额和关键业务命令必须使用数据库事务，并原子写入审计与 outbox。
- AI 仅通过受控应用工具工作，不能直连数据库或绕过权限、状态机与人工批准。
- 纵向切片必须同时覆盖 migration、领域逻辑、API、前端、权限、审计和测试。
- 只借鉴参考项目的流程与对象设计；引入代码或依赖前复核许可证。

## 变更与验证

- 保留用户已有文件和未提交修改；禁止破坏性 reset、checkout 或 clean。
- 架构偏离先写 ADR，再修改实现与指南。
- 完成前运行与改动相称的格式化、lint、类型检查、测试和容器配置/启动验证。
- 最终报告必须区分已验证、未验证、风险和下一步；代码写完不等于任务完成。
