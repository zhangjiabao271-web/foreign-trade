# 组织设置与成员授权

沿用工作台现有字体、色彩、表单和分页组件样式。页面的唯一任务是让当前组织管理员核对并管理授权；不添加营销式展示或远程登录账号管理。

遵循 ADR-018。服务端数据使用 TanStack Query 和生成客户端，表单使用 RHF/Zod。表单打开时冻结目标版本；未经修改的失败重试复用幂等键。自我降级/停用成功后刷新权限并卸载管理界面。添加成员必须核实准确 Logto subject；不收集密码。

技能检索建议的营销落地页结构和 Server Actions 不适用本项目；以实施指南的后端命令、生成 API 客户端与现有工作台视觉基线为准。
